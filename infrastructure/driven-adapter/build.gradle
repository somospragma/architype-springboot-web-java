plugins {
    id 'java-library'
}

dependencies {
    // Dependencias comunes para todos los adaptadores
    implementation 'org.springframework.boot:spring-boot-starter'
    implementation project(':domain')
}

